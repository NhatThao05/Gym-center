import { Typography } from "@mui/material";
import {styled} from "@mui/system";
import { Colors } from "../theme";

export const FooterLine = styled(Typography)(() => ({
 color: Colors.white,
 display: 'flex',
 width: '50%',
}))

